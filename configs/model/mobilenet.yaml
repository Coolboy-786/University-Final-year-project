_target_: src.models.mobilenet.MobileNetV2Classifier

num_classes: ???  # filled from class_mapping at runtime
pretrained: true
unfreeze_last_n: 20  # matches thesis §10.3.2

optimizer:
  _target_: torch.optim.Adam
  lr: 1.0e-4

scheduler: null

trainer_overrides:
  max_epochs: 15
